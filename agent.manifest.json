{
  "$schema_note": "Machine-readable map of this template for AI agents customizing it per client. Keep in sync with data-section/data-role attributes in src/components/*.tsx — if you add, remove, or rename a section/role in the code, update this file in the same change.",
  "framework": "Next.js (App Router) + Tailwind CSS v4",
  "locale": {
    "lang": "en",
    "secondary": ["el"],
    "bilingual": true,
    "note": "Bilingual: base language English (en), secondary Greek (el). ALL visible copy comes from the dictionaries in src/content/en.json and src/content/el.json (imported by src/lib/i18n.tsx, one key per string, both languages filled) — components read it via useT()/t('key') and are all client components. layout.tsx wraps the app in <LanguageProvider> (defaults to en, persists to localStorage, sets <html lang>). Header.tsx renders the language switcher (data-role='lang-switch').",
    "editingCopy": "Change the value in BOTH src/content/en.json and src/content/el.json. Never hardcode a visible string back into JSX — that breaks the other language. See AGENTS.md §5.",
    "addingALanguage": "Add a new src/content/{locale}.json file (same keys) and import it into the translations object in src/lib/i18n.tsx, plus a button for it in the Header.tsx switcher. Do NOT add an i18n block to next.config.ts (Pages Router only) or [lang] route folders unless separate per-language URLs are explicitly requested."
  },
  "font": "DM Sans, loaded via next/font/google in src/app/layout.tsx",
  "pageComposition": {
    "file": "src/app/page.tsx",
    "order": ["header", "hero", "services", "about", "cta", "footer"]
  },
  "assets": {
    "imagesDir": "public/images",
    "urlPrefix": "/images/",
    "note": "Files in public/images are served from site root (public/images/foo.jpg -> /images/foo.jpg). No component currently reads from this folder — see public/images/README.md and AboutSection.tsx's placeholder div, the first candidate to wire up to a real image."
  },
  "brand": {
    "name": {
      "value": "Our Company (en) / Η Εταιρεία Μας (el)",
      "locations": [
        "src/content/en.json + src/content/el.json (brand key) — Header logo and Footer logo/copyright read it via t('brand')",
        "src/app/layout.tsx (metadata.title — literal 'Our Company'; metadata can't use the hook)"
      ],
      "note": "No longer hardcoded in the components. For a rebrand, change the 'brand' key in BOTH src/content/en.json and src/content/el.json, plus the literal metadata.title in layout.tsx."
    },
    "color": {
      "token": "--color-brand defined in src/app/globals.css (@theme inline), value #0070f3",
      "status": "NOT WIRED UP — no component actually uses the brand-* utility classes. Every component hardcodes the literal arbitrary value text-[#0070f3] / bg-[#0070f3] instead.",
      "literalLocations": [
        "src/app/globals.css",
        "src/components/Header.tsx",
        "src/components/HeroSection.tsx",
        "src/components/ServicesSection.tsx",
        "src/components/AboutSection.tsx",
        "src/components/CTASection.tsx"
      ],
      "note": "A full brand-color change currently requires a literal find/replace of '#0070f3' across all files listed above, not just an edit to globals.css."
    },
    "contactInfo": {
      "email": "info@example.com",
      "phone": "+30 210 123 4567",
      "address": "Αθήνα, Ελλάδα",
      "locations": [
        "src/components/CTASection.tsx (email + phone, as mailto:/tel: links)",
        "src/components/Footer.tsx (email + phone + address, as plain text)"
      ]
    }
  },
  "scrollAnchors": {
    "note": "Section ids are targeted by nav hrefs (#id). If a section's id changes, update every href that points to it.",
    "about": { "id": "about", "definedIn": "src/components/AboutSection.tsx", "linkedFrom": ["Header.tsx nav", "Footer.tsx nav"] },
    "services": { "id": "services", "definedIn": "src/components/ServicesSection.tsx", "linkedFrom": ["Header.tsx nav", "Footer.tsx nav", "HeroSection.tsx secondary CTA"] },
    "contact": { "id": "contact", "definedIn": "src/components/CTASection.tsx", "linkedFrom": ["Header.tsx nav + CTA buttons", "Footer.tsx nav", "HeroSection.tsx primary CTA"] }
  },
  "roleNamingConvention": "data-role values describe FUNCTION, not location, and are reused across sections/components by design (e.g. multiple elements can share data-role=\"nav-link\" or data-role=\"contact-button\"). Always scope a data-role lookup within its nearest data-section ancestor.",
  "sections": {
    "header": {
      "file": "src/components/Header.tsx",
      "shared": true,
      "note": "Rendered once in page.tsx, outside <main>. Client component (has mobile menu state).",
      "roles": {
        "logo": "Link to '/' showing the brand name",
        "nav-link": "One per navLinks entry; rendered TWICE (desktop nav + mobile dropdown), each occurrence tagged",
        "contact-button": "Link to #contact; rendered TWICE (desktop pill button + mobile dropdown link)",
        "menu-toggle": "Mobile hamburger button, no href, toggles the dropdown",
        "lang-switch": "Language toggle (EN | EL); rendered TWICE (desktop + mobile dropdown). Calls setLang from useT() in src/lib/i18n.tsx"
      },
      "contentArrays": {
        "navLinks": {
          "definedAt": "top of file",
          "shape": "{ labelKey: TranslationKey, href: string }[] — label comes from the dict via t(labelKey)",
          "currentItems": 3
        }
      }
    },
    "hero": {
      "file": "src/components/HeroSection.tsx",
      "roles": {
        "title": "h1 headline",
        "subtitle": "p supporting copy under the headline",
        "contact-button": "Primary CTA, links to #contact",
        "cta-button": "Secondary CTA, links to #services"
      }
    },
    "services": {
      "file": "src/components/ServicesSection.tsx",
      "roles": {
        "title": "Section heading",
        "subtitle": "Section intro paragraph",
        "service-item": "Card wrapper, one per services[] entry",
        "item-title": "Card heading, inside each service-item",
        "item-description": "Card body text, inside each service-item"
      },
      "contentArrays": {
        "services": {
          "definedAt": "top of file",
          "shape": "{ titleKey: TranslationKey, descKey: TranslationKey, icon: JSX <path> element }[] — title/description come from the dict via t(); only the icon is inline",
          "currentItems": 3
        }
      }
    },
    "about": {
      "file": "src/components/AboutSection.tsx",
      "roles": {
        "title": "h2 heading",
        "subtitle": "First supporting paragraph only"
      },
      "untaggedContent": {
        "eyebrow span": "Small label above the heading (t('about.eyebrow')), no enumerated role fits it",
        "second paragraph": "Additional body copy after the subtitle paragraph (t('about.p2'))",
        "highlights list": "highlightKeys: TranslationKey[] const at top of file, rendered as <li> items via t(key), not individually tagged",
        "image placeholder div": "Marked in-code with a comment to replace with next/image when a real photo exists"
      }
    },
    "cta": {
      "file": "src/components/CTASection.tsx",
      "note": "This is the id=\"contact\" section — the template's 'contact' section is named cta, not contact, in data-section.",
      "roles": {
        "title": "h2 heading",
        "subtitle": "Supporting paragraph",
        "email-link": "mailto: link",
        "phone-link": "tel: link"
      }
    },
    "footer": {
      "file": "src/components/Footer.tsx",
      "shared": true,
      "note": "Rendered once in page.tsx, after <main>.",
      "roles": {
        "logo": "Brand name span (not a link, unlike the header logo)",
        "nav-link": "3 nav links rendered via navLinks.map (array-driven, same as Header)",
        "email-link": "Plain text <p>, not an actual mailto: anchor",
        "phone-link": "Plain text <p>, not an actual tel: anchor",
        "address": "Plain text <p>"
      },
      "untaggedContent": {
        "tagline paragraph": "Short description under the logo",
        "copyright line": "Auto-generates the year via new Date().getFullYear()",
        "'Powered by agapps.eu' credit link": "Template attribution, not client content — generally leave this untouched when customizing for a client"
      }
    }
  }
}
