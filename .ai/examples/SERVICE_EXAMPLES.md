---
status: template
tier: 3
last_verified: never
verified_against: none
---

# Service Examples

> TEMPLATE — each entry documents a real service/business-logic module in
> this project. "Service" here means whatever this project's own
> convention calls its business-logic layer — see `../knowledge/MODULE_MAP.md`.

## Example: [real service/module name]

**Context**: [what real problem this service exists to solve, and what
calls into it.]

**Analysis**: [why it's structured the way it is — what alternative was
considered/rejected, if known.]

**Implementation**: [the real shape — real function signatures, real
error-handling approach, real external calls it makes.]

**Validation**: [how this service is actually tested.]

**Common Errors**: [a real mistake to avoid when writing a similar
service in this project — e.g. a wrong assumption about error handling,
a missed edge case.]

---

[Repeat for each real service worth documenting as a reference example.]
