---
status: template
tier: 3
last_verified: never
verified_against: none
---

# API Examples

> TEMPLATE — each entry documents a real, existing API endpoint/command in
> this project, chosen because it's a clean/representative or particularly
> instructive real example. See `../knowledge/API_STRUCTURE.md` for the
> general shape this should follow.

## Example: [real endpoint/command name]

**Context**: [what this real endpoint/command does and why it's a good
example to learn from.]

**Analysis**: [what this endpoint needed to check/validate, and why —
auth, ownership, input shape, business rules.]

**Implementation**: [the real code shape — auth/validation/logic/response,
as actually structured in this project, with a real (or closely
paraphrased, structurally faithful) code excerpt.]

**Validation**: [how this endpoint is actually tested/verified in this
project.]

**Common Errors**: [a real mistake this endpoint's design specifically
avoids, or a real bug once found in a similar endpoint.]

---

[Repeat for each real endpoint worth documenting as a reference example.]
